# 粉笔页面存档：含其前端资源与题库内容，不随仓库分发
# 用通配符而不是写死文件名，换一套题存档时不用再改这里
专项智能练习*背题.html
专项智能练习*背题_files/

# 脚本的本地工作副本，发布版是 fenbi-memorize-layout.user.js
粉笔刷题-背题页面布局优化-*.txt

.DS_Store

# 「题目脱库」导出来的文件，落在脚本旁边。文件名是「页面标题_YYYYMMDD-HHMM」，
# 里面是整份真题（题干、选项、答案解析），和上面那些存档一个性质。
# 认这个时间戳后缀，而不是 *.json —— 那样会把 package.json 一起挡掉。
*_20[0-9][0-9][0-9][0-9][0-9][0-9]-[0-9][0-9][0-9][0-9].json
*_20[0-9][0-9][0-9][0-9][0-9][0-9]-[0-9][0-9][0-9][0-9].md

# 测试夹具：由 test/make-fixture.mjs 从题库存档生成，同样含题库内容
node_modules/
test/fixtures/

# Chrome 加载未打包扩展时自己生成的，不是源码
fenbi-ai-unframe/_metadata/
